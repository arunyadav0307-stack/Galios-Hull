# Phase 10C — Global Product Repair Audit

**Audit date:** 2026-09-24 (Asia/Calcutta)
**Repository:** `arunyadav0307-stack/Galios-Hull`
**Branch:** `arena/01a0c9d2-galios-hull`
**Mandatory base:** `92a7eb2ba6472b95fe9306fb76e762477c836c01`
**Previous audit preserved:** `validation/PHASE10B_REFEREE_REPORT.md`

## 1. Verdict

**Phase-10C mathematical and validation verdict: COMPLETE.** The M1 blocker reported by Phase 10B is repaired in the manuscript and independently exercised by a new exhaustive direct-product validator. The global affine-product algebra, tuple-coordinate ambient module, product code, componentwise Frobenius, global A-valued code-first pairing, both inclusions of the global dual decomposition, the global hull decomposition, and conversion to global `F_q` dimensions are now explicit.

The theorem and corollary chain was re-audited after the repair. The reciprocal and ordinary constacyclic-reciprocal proof expansions, orbit orientation, transfer trace, uniform moment model, citation scope, dependency graph, and four-table plan are consistent with the repaired statement. All available Python validation passes. LaTeX compilation was **not performed** because `pdflatex` and `bibtex` are unavailable in the environment; no compilation success is claimed.

The final release gate is: commit with the exact message `Repair global affine-product pairing and hull decomposition`, push only to `origin/arena/01a0c9d2-galios-hull`, and verify equal local/remote HEADs and a clean worktree.

## 2. Repair scope and preserved material

The Phase-10C starting point was the mandatory base commit above on the required branch. The following existing evidence and mathematics were preserved:

- `validation/PHASE10B_REFEREE_REPORT.md` and all earlier validation artifacts;
- component code-first duality, inverse-Frobenius reciprocal, root action, compatibility criterion, hull-support orientation, orbit enumeration, transfer matrix, variance calculation, and F4/F8/F16/N2 results;
- the fixed code-first convention
  `\langle x,y\rangle_{s,k}=\sum_i x_i y_i^{p^k}`;
- the distinction between base field `F_q`, component fields `K_s=F_{q^{m_s}}`, and the global algebra `A`;
- the labeled-selection interpretation, with no quotient by rotations, automorphisms, isometries, equivalence classes, or orbit classes.

No bibliography entry was added or altered. The transfer-matrix mechanism is proved by direct closed-walk expansion in the manuscript rather than assigned an unsupported literature characterization.

## 3. Global algebra and module repair

`manuscript/main.tex` now fixes the labeled CRT isomorphism

```text
A ~= product_s K_s,       K_s = F_{q^{m_s}},       [K_s:F_q] = m_s,
```

and explicitly treats elements of `A` as fixed labeled tuples. It defines

```text
lambda = (lambda_s)_s in A^*
M_lambda = A[x]/(x^n-lambda)
        ~= product_s K_s[x]/(x^n-lambda_s)
        ~= product_s K_s^n.
```

The global code is explicitly

```text
C(J) = product_s C_s(J_s) subseteq A^n,
```

and is also identified with the ideal generated by the tuple of component generators. The proof of Lemma `lem:global-code` uses the component factor-selection bijection to prove injectivity of distinct labeled selections. Thus coefficient extraction counts distinct labeled codes, not equivalence classes or quotient orbits.

## 4. Global Frobenius, pairing, dual, hull, and dimensions

The repaired manuscript includes the following dependency bridge in Section 3, also recorded in `validation/THEOREM_DEPENDENCY_GRAPH.md`:

```text
Proposition 3.1  square-free affine decomposition
       -> Lemma 3.2       component factor selections
       -> Lemma 3.3       global CRT ambient module and code
       -> Lemma 3.4       global Frobenius and A-valued pairing
       -> Lemma 3.5       global code-first dual decomposition
       -> Lemma 3.6       global hull decomposition
       -> Proposition 3.7  global F_q dimension additivity
       -> Theorem 5.1      component hull support
       -> Theorem 8.1      global labeled enumerator
       -> Section 9        corollaries
```

The global maps are defined componentwise:

```text
sigma_A,k((a_s)_s) = (sigma_s,k(a_s))_s,
rho_A,k((a_s)_s)   = (rho_s,k(a_s))_s.
```

The global pairing is explicitly A-valued and componentwise, not an `F_q` trace pairing:

```text
<x,y>_{A,k} = sum_i x_i sigma_A,k(y_i)
             = (sum_i x_{s,i} y_{s,i}^{p^k})_s.
```

The global code-first dual and hull are defined by this pairing and `0_A`. Lemma `lem:global-dual` proves both inclusions:

1. For a global dual word, a single-component-supported codeword `(c_s,0,...)` forces its `s`-component into the component code-first dual.
2. If every component lies in its component dual, every component of the tuple pairing is zero, so the global pairing is `0_A`.

Lemma `lem:global-hull` then derives

```text
Hull_A,k(C(J)) = product_s Hull_k(C_s(J_s))
```

from the dual identity and the elementary intersection identity for finite direct products. Proposition `prop:global-dimensions` converts component dimensions using `[K_s:F_q]=m_s` and records the orbit weight

```text
w_O = m_s d_O.
```

This closes the precise M1 gap identified by Phase 10B.

## 5. Theorem 8.1 and downstream corollary audit

Theorem 8.1 now states, rather than assumes implicitly, `A`, the fields `K_s`, the tuple twist, the product code, the global A-valued dual/hull definitions, the compatibility condition, the global code dimension, and the global hull dimension. Its proof cites the repaired global lemmas before invoking component hull support and the orbit statistic.

The downstream results were checked as follows:

- **Total labeled-code count:** `E(1,1)=2^{sum_s |F_s|}` follows from the injective labeled selection space.
- **Code-dimension distribution:** each unselected factor contributes `m_s deg(f)` over `F_q`; setting `z=1` gives the displayed factor product.
- **Hull-dimension distribution:** setting `u=1` uses the repaired global hull and the local orbit polynomial.
- **LCD count:** zero forward boundary on every orbit is equivalent to a constant binary word on every orbit; the count is `2^{sum_s |O_s|}` over labeled selections.
- **Mean:** the manuscript now explicitly declares the uniform measure on the distinct labeled selection space, with probability `1/E(1,1)` per selection/code. Each directed boundary has expectation `1/4`; length-one orbits contribute zero.
- **Variance:** the length-two mutual-exclusion exception remains separate (`w^2/4`), and lengths at least three contribute `a w^2/16`. Independent orbit contributions add.

The orientation remains the code-first forward boundary `epsilon_i(1-epsilon_{i+1})`, consistent with `tau(J_s) setminus J_s`; no inverse-orientation replacement was made.

## 6. Reciprocal and constacyclic proof audit

The normalized inverse-Frobenius reciprocal proof now explicitly covers:

- nonzero leading and constant coefficients;
- monicity and degree preservation;
- multiplicativity of reversal, coefficient automorphisms, and normalization;
- the inverse construction using the corresponding `sigma` normalization;
- preservation of irreducibility under coefficient automorphisms and ordinary reciprocal reversal.

The ordinary constacyclic reciprocal proof now supplies the coefficient/shift calculation for non-wrapped generator shifts, identifies the relevant coefficients of `GH=x^n-a`, establishes the inverse-twist modulus via the normalized reciprocal of `M`, and uses the dimension/codimension argument to identify the full dual ideal. The code-first theorem still uses the inverse-Frobenius reciprocal and the transformed twist `lambda_s^{-p^{d_s-k}}`.

The candidate-first convention remains separate:

```text
D_candidate(C) = sigma_k^2(D_code-first(C)).
```

No equality of candidate-first and code-first dual subspaces or supports is claimed.

## 7. Orbit, transfer, and enumeration audit

No orbit enumeration or transfer matrix was redesigned. The manuscript still uses the weighted two-state matrix

```text
T_w(u,z) = [[u^w, 1], [u^w z^w, 1]],
```

with destination weighting and cyclic trace. The former unsupported description of the transfer construction as a literature-backed “standard combinatorial tool” was removed from the manuscript introduction. The local trace identity is proved directly by expanding closed walks; the literature records remain conservative and scoped.

The global test compares three independent objects for every labeled product selection:

1. direct global pairing/nullspace and hull dimensions;
2. the product of component histograms;
3. the orbit-boundary product and the transfer-matrix product.

All three agree exactly.

## 8. New exhaustive global-product validator

Added: `code/validate_global_product.py`.

The validator uses the compatible nontrivial affine-product instance

```text
A = F_4 x F_16 over F_4,
K_1=F_4 (m_1=1), K_2=F_16 (m_2=2),
n=5, lambda=(omega,1), k=1.
```

The F4 component has three factors with orbit data `[(1,1),(2,2)]`; the F16 component splits into five linear factors with orbit data `[(1,2),(4,2)]`. Thus the labeled selection space has `2^3 * 2^5 = 256` selections.

The exhaustive run reports:

- `256` distinct canonical global code spaces;
- `256` direct global dual nullspace/decomposition checks;
- `256` global `F_4` code/hull dimension-additivity checks;
- `1280` global affine-product shift checks on component bases;
- direct global histogram equals the component-product histogram;
- direct global histogram equals the orbit-boundary histogram;
- orbit-boundary histogram equals the transfer histogram;
- uniform mean `3.0` and variance `2.0` for this test instance;
- final line: `PASS: exhaustive global-product and global-enumerator validation`.

The global nullspace is built independently by expanding the A-valued pairing equations to binary equations and then converting the resulting dimensions back to `F_4` dimensions. The test also exhaustively exercises tuple algebra laws, tuple Frobenius, the A-valued distinction between component coordinates, factorization, square-freeness, reciprocal action, injectivity, and the componentwise affine shift.

## 9. Existing validation regression

The combined capture is `validation/phase10c_full_validation.out`. It preserves the output of the new global test and reruns all available validators:

```text
python code/validate_global_product.py
python code/validate_pilot.py
python code/validate_long_orbit_examples.py
python code/validate_nontrivial_constacyclic.py
python code/validate_n2_extension_convention.py
python code/diagnose_incompatible_twist.py
python code/diagnose_n2_incompatible_twist.py
python -m py_compile code/*.py
```

Results:

- F4 pilot: PASS; all 8 component selections and the 4096 four-component product histogram agree.
- F8 long orbit: PASS; all 128 selections, including the length-six orbit, agree with theory and transfer.
- F16 extension example: PASS; all 32 selections and the length-four orbit agree.
- Compatible nontrivial twist: PASS; all 8 selections agree.
- N2-A: PASS; all 32 direct duals select the principal inverse-Frobenius reciprocal; the alternative is only a comparator.
- Incompatible basic twist: diagnostic only and correctly excluded from the same-factor-set transfer theorem.
- N2-B incompatible extension: PASS as a diagnostic; principal predicted twist agrees and the alternative does not generally.
- Python compilation: `PY_COMPILE_PASS`.

No existing validator was modified.

## 10. Manuscript structure, tables, and citations

The manuscript retains exactly four data tables and five figure placeholders. The table-plan amendment explicitly resolves the earlier five-versus-four mismatch: the dependency map remains a validation artifact, not a fifth manuscript data table. The computational table now includes the Phase-10C global-product bridge as a row within Table 4.

`validation/THEOREM_DEPENDENCY_GRAPH.md` records the new global bridge and preserves the separate convention-transformation and Gram-invariance branches. `validation/MANUSCRIPT_TABLE_PLAN.md` records the four-table resolution and the new global validation row.

`manuscript/references.bib` is unchanged. Static citation-key checking covers all 13 cited keys with no missing key. No citation is used to claim an exact transfer-matrix literature theorem, priority, firstness, uniqueness, or absence. The literature comparison remains scoped to the verified/partially verified records and the convention mismatch already documented in the prior audits.

## 11. Static checks and compilation boundary

The full capture reports:

- balanced LaTeX environments;
- zero brace imbalance;
- no duplicate labels;
- no undefined internal references;
- no missing citation keys;
- exactly four tables and five figures;
- `git diff --check` pass.

`pdflatex` and `bibtex` are unavailable. Compilation was not performed, and this audit makes no PDF, bibliography-build, or typesetting claim.

## 12. Release gate

The mathematical repair is complete and the validation gate passes. The required release protocol is:

1. remove only generated `__pycache__` material;
2. inspect the final diff and status;
3. commit exactly with message `Repair global affine-product pairing and hull decomposition`;
4. push only `arena/01a0c9d2-galios-hull` to `origin`;
5. verify local HEAD equals `origin/arena/01a0c9d2-galios-hull` and `git status --short` is empty.

The final delivery records the resulting commit and local/remote verification. All earlier validation evidence, including `PHASE10B_REFEREE_REPORT.md`, must remain present after the release gate.
